import React from 'react';
import { createComponent } from 'react-fela';

const main = () => ({});

const $Home = createComponent(main);

const Home = () => (
	<$Home >
    Home
	</$Home >
);

export default Home;
